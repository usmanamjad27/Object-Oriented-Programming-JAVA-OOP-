public interface Q04_Enumeration {
    // return true if a value exists in the next index
    boolean hasNext(int index);

    // returns the next element in the collection as an Object
    Object getNext(int index);
}
